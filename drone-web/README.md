# Assignment #2: Frontend (Web Application)

ยินดีต้อนรับสู่ส่วน **Frontend (Web App)** ของโพรเจกต์ **DriveSphere** ส่วนนี้พัฒนาโดยใช้ **Next.js**, **TypeScript**, และ **Tailwind CSS** เพื่อสร้างเว็บแอปที่สามารถดูและบันทึกข้อมูล Log ของโดรนได้อย่างสะดวกและปลอดภัย  



## Features

* **Drone Configuration Page**  
  แสดงข้อมูลพื้นฐานของโดรน เช่น `drone_id`, `drone_name`, `light`, `country`, `weight` โดยดึงข้อมูลจาก API ของ Backend  

* **Drone Status Page**  
  แสดงสถานะการทำงาน (`condition`) ของโดรนแบบเรียลไทม์  

* **Log Viewer Page**  
  แสดง Log ของโดรนในรูปแบบตาราง พร้อมระบบ **Pagination** สามารถดูข้อมูลย้อนหลังได้ทีละ 12 รายการ  

* **Add New Log Page**  
  ฟอร์มสำหรับเพิ่มข้อมูล Log ใหม่ เช่น อุณหภูมิ (`celsius`) โดยส่งข้อมูลผ่าน `POST /logs` ไปยัง Backend  

* **Responsive Design**  
  ออกแบบด้วย **Tailwind CSS** ให้เหมาะกับทั้งจอ Desktop และ Mobile  



## Tech Stack

| ส่วน | เทคโนโลยี | รายละเอียด |
| :--- | :--- | :--- |
| **Framework** | Next.js 15 | ใช้สำหรับสร้างเว็บแอปแบบ SSR / SSG |
| **Language** | TypeScript | เพิ่มความปลอดภัยและลดข้อผิดพลาดในโค้ด |
| **UI Framework** | Tailwind CSS | สำหรับตกแต่งและจัด layout หน้าเว็บ |
| **API** | Express.js (Backend) | เชื่อมต่อกับ API จาก Assignment #1 |
| **Deployment** | Vercel | สำหรับ Deploy เว็บแบบ Static Hybrid |
| **Version Control** | Git & GitHub | สำหรับจัดการเวอร์ชันของโค้ด |



## การติดตั้งและรันโปรเจกต์ (Frontend)

1. **โคลนโปรเจกต์จาก GitHub**  (หากยังไม่ได้ clone ใน Assignment #1 )
   ```bash
   git clone https://github.com/septemwee/DriveSphere.git

2. **เข้าสู่โฟลเดอร์โปรเจกต์**
    ```bash
    cd drone-web
    ```

3. **ติดตั้ง Dependencies**
    ```bash
    npm install
    ```

4. **สร้างไฟล์ Environment Variables**
    สร้างไฟล์ชื่อ `.env.local` ภายในโฟลเดอร์ `drone-web` แล้วเพิ่มค่าต่อไปนี้ (ใช้ URL ของ Backend จริง)

    * **localhost** เรียก server ที่เปิดใน Assignment #1
    ```env
    NEXT_PUBLIC_API_URL=http://localhost:3001
    NEXT_PUBLIC_DRONE_ID=66010051
    ```
    * **Production (Vercel)** เรียก server ที่ deploy บน Vercel
    ```env
    NEXT_PUBLIC_API_URL=https://drive-sphere-server.vercel.app
    NEXT_PUBLIC_DRONE_ID=66010051
    ```


5. **รันเว็บแอป**
    ```bash
    npm run dev
    ```
แอปจะรันที่  
🔗 `[http://localhost:3000](http://localhost:3000)`



## Deployment

แอปนี้ถูก Deploy บน **Vercel** และเชื่อมต่อกับ Backend (Assignment #1 or Vercel) โดยตรงผ่าน Environment Variables  

🔗 [**Frontend Live on Vercel**](https://drive-sphere-web.vercel.app)



## การเชื่อมต่อกับ Backend

* Frontend จะเชื่อมกับ API ผ่านตัวแปร `NEXT_PUBLIC_API_URL`
* ใช้ฟังก์ชัน `fetch()` ดึงข้อมูลจาก Backend เช่น

  ```ts
  const apiUrl = process.env.NEXT_PUBLIC_API_URL;
  const droneId = process.env.NEXT_PUBLIC_DRONE_ID;

  const response = await fetch(`${apiUrl}/configs/${droneId}`, { cache: "no-store" });
  if (!response.ok) {
    throw new Error(`Failed to fetch: Server responded with ${response.status}`);
  }
  return response.json();
  ```
* เมื่อ Deploy บน Vercel ต้องตั้งค่า Environment Variables ใน Dashboard ของ Vercel ด้วย

## หมายเหตุ

* ใช้ Next.js 15 + TypeScript + TailwindCSS
* รันแบบ Hybrid (Static / Server Components)
* รองรับการใช้งานทั้งบนเครื่อง (localhost) และบน Production 